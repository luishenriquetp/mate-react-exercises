/*
Renderize os itens de db.json como uma lista (<ul/>) utilizando mapping.

Com os seguintes requisitos:

- Adicione um Input para digitar o valor a ser filtrado (o valor deve ser filtrado se estiver incluido no campo, e deve ser case insensitive)
- Adicione um  Select com os possiveis campos para filtro (chaves do objeto do array) 
- O filtro não deve precisar de botão para funcionar
*/

export default function MapFilterList() {
  return (
    <div>
      <h2>Map Filter Sorting</h2>

      {/* Seu código vai aqui */}
    </div>
  );
}

